# mayue 仓库协作说明

## 项目定位

这是一个由多个独立 HTML 小工具组成的个人产品仓库。每个产品通常位于自己的目录中，并以 `index.html` 作为入口。

## 目录与产品

- `index.html`：MAYUE LABS 项目首页和产品入口
- `badminton-coach/`：羽毛球教学工具
- `life-card/`：人生进度
- `personal-page/`：个人主页
- `prompt-manager/`：提示词管理
- `rhythm/`：节律记录
- `time-perception/`：时间感知
- `trilingual-cards/`：三语卡片
- `tv-clock/`：TV 端时钟
- `wealth-tracker/`：财务追踪

## 修改原则

1. 先明确本次修改的单一目标、影响页面和验收标准，再开始编辑。
2. 默认只修改用户指定的产品和相关文件，不要顺手重构其他页面。
3. 保持现有产品的核心功能、文案、数据结构和视觉方向；如需改变，先说明原因。
4. 每个产品目前以单文件 HTML 为主。除非确有必要，不要引入构建系统、框架或额外依赖。
5. 注意已有的 `localStorage`、外部 API、导出能力和第三方 CDN 依赖，不要因视觉调整误删或破坏它们。
6. 保持提交粒度清晰：一个提交尽量只解决一个产品问题或一个明确主题。

## 视觉与响应式要求

- 优先沿用目标页面已有的颜色、字体、间距、圆角和动效语言。
- 修改桌面布局时，同时检查窄屏和手机布局，避免溢出、遮挡和不可点击。
- 图片、头像和嵌入资源应考虑文件体积；除非已有约定，不要无必要地把大资源直接内嵌进 HTML。
- 交互控件应保持清晰的可点击状态、键盘焦点和足够的对比度。

## 完成前验收

修改 HTML 后，至少检查：

1. 页面能正常打开，没有明显 HTML、CSS 或 JavaScript 错误。
2. 桌面端和移动端布局都正常，没有横向滚动或内容溢出。
3. 页面中的主要按钮、链接、表单、弹窗和导出功能仍然可用。
4. 使用 `localStorage` 或外部 API 的页面，在空数据、刷新和请求失败时不会直接崩溃。
5. 如果修改了首页、产品目录或新增产品，检查首页入口和 README 产品列表是否同步。
6. 查看 Git diff，确认没有意外修改其他产品或生成临时文件。

## 文档同步规则

新增、删除或重命名产品时，需要同步检查：

- 根目录 `index.html` 的入口和编号
- `README.md` 的产品列表与简介
- 产品目录中的 `index.html`
